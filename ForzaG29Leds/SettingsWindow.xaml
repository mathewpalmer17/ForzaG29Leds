<Window x:Class="ForzaG29Leds.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Forza Rev LED Settings"
        Width="420" Height="540"
        MinWidth="380" MinHeight="480"
        ResizeMode="CanResize"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="True">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="#1A1A1A"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="16,6"/>
        </Style>
        <Style x:Key="SectionHeader" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#444"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>
    </Window.Resources>

    <TabControl Margin="0">

        <!-- ── Settings tab ─────────────────────────────────────────────── -->
        <TabItem Header="Settings">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="16,16,16,8">

                        <!-- Connection -->
                        <TextBlock Text="CONNECTION" Style="{StaticResource SectionHeader}"/>
                        <Border BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4" Padding="12" Margin="0,0,0,16">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="130"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="UDP Port"/>
                                <TextBox x:Name="PortBox" Grid.Column="1" Width="80" HorizontalAlignment="Left"/>
                            </Grid>
                        </Border>

                        <!-- Rev Lights -->
                        <TextBlock Text="REV LIGHTS" Style="{StaticResource SectionHeader}"/>
                        <Border BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4" Padding="12" Margin="0,0,0,16">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="130"/>
                                    <ColumnDefinition Width="80"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="34"/>
                                    <RowDefinition Height="34"/>
                                    <RowDefinition Height="34"/>
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0" Text="Solid from"/>
                                <TextBox   Grid.Row="0" Grid.Column="1" x:Name="SolidBox" Width="60" HorizontalAlignment="Left"/>
                                <TextBlock Grid.Row="0" Grid.Column="2" Text="% of max RPM" Margin="8,0,0,0"/>

                                <TextBlock Grid.Row="1" Text="Flash from"/>
                                <TextBox   Grid.Row="1" Grid.Column="1" x:Name="FlashBox" Width="60" HorizontalAlignment="Left"/>
                                <TextBlock Grid.Row="1" Grid.Column="2" Text="% of max RPM" Margin="8,0,0,0"/>

                                <TextBlock Grid.Row="2" Text="Flash speed"/>
                                <TextBox   Grid.Row="2" Grid.Column="1" x:Name="FlashMsBox" Width="60" HorizontalAlignment="Left"/>
                                <TextBlock Grid.Row="2" Grid.Column="2" Text="ms on/off" Margin="8,0,0,0"/>
                            </Grid>
                        </Border>

                        <!-- Status -->
                        <TextBlock Text="STATUS" Style="{StaticResource SectionHeader}"/>
                        <Border BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4" Padding="12">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                    <Ellipse x:Name="WheelDot" Width="10" Height="10" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                    <TextBlock x:Name="WheelLabel"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <Ellipse x:Name="TelDot" Width="10" Height="10" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                    <TextBlock x:Name="TelLabel"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>

                    </StackPanel>
                </ScrollViewer>

                <!-- Buttons pinned to bottom -->
                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="16,8,16,12">
                    <Button Content="Save"   Width="80" Margin="0,0,8,0" Click="Save_Click" IsDefault="True"/>
                    <Button Content="Cancel" Width="80" Click="Cancel_Click" IsCancel="True"/>
                </StackPanel>
            </Grid>
        </TabItem>

        <!-- ── Telemetry tab ─────────────────────────────────────────────── -->
        <TabItem Header="Telemetry">
            <Grid>
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                              Background="#FAFAFA">
                    <TextBlock x:Name="TelemetryDump"
                               FontFamily="Consolas"
                               FontSize="11"
                               Foreground="#222"
                               Margin="12"
                               TextWrapping="NoWrap"
                               Text="Waiting for telemetry…"/>
                </ScrollViewer>
            </Grid>
        </TabItem>

    </TabControl>
</Window>
